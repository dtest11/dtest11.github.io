```bash

update `user` set hongbao_id = 100 where id=1;
create table  user ( id int auto_increment primary key, value int,hongbao_id int  )
create index idx_sex on sex (sex);


create table  user ( id int auto_increment primary key, value int,hongbao_id int  )


create table  sex ( id int auto_increment primary key, name varchar(10),age int,sex int  );

```